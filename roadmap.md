# Roadmap

Living list. Not a promise of dates. Not a consensus roadmap.

## Now

- Public README (no workdir paths).
- Tree: `docs/` normative, `research/` adjacent, `src/` reserved empty.
- Landscape current through Bit-Block V2, Monetary Node strip demo, Bitcoin Purity boundary case.

## Next (documentation)

- Land template-generation note (`research/template-generation-dmtg.md`).
- Keep quantum note updated when BIP status or resource estimates actually change.
- Issue templates for landscape updates so the map does not rot.

## Later (only after the above is stable)

- Default-value tables (policy knobs, not consensus).
- Measurement scripts under `scripts/`.
- First code in `src/` — most likely a defaults / configuration layer, not a clean-room node.

## Explicitly not on this roadmap

- Hard fork of the primary line.
- 32 MB blocks as “quantum readiness.”
- Shipping a binary because the folder exists.
